#include <stdio.h>
#include <stdlib.h>


int main()
{

   FILE  *open;
   char takim;
   open=fopen("takimlar.txt","r");

   do{

   takim=getc(open);

   printf("%c",takim);
   }

   while(takim!=EOF);

   fclose(open);

return 0;
}
